# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bbf110a0bf

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "pet_post_success.csv"
    fields:
      - "petName"
      - "petType"
    skipHeader: true

scenarios:
  - name: "Add a new pet to the store"
    flow:
      - log: "Adding a new pet to the store"
      - post:
          url: "/pet"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            name: "{{ petName }}"
            type: "{{ petType }}"
          expect:
            - statusCode: 200
            - hasProperty: "petId"
            - hasProperty: "petName"
            - hasProperty: "petType"
          # We expect a 200 status code and the response to contain a petId, petName and petType.
          # The petId is generated by the server and the petName and petType should match the ones we sent.
      - think: 1
