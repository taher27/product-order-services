# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=d9b91c8c5d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "checkout-cart-anonymous_id_post_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Checkout cart anonymous API test"
    flow:
      - log: "Test Case 1: Testing with invalid id in the path"
        post:
          url: "/checkout-cart-anonymous/invalidId"
          headers:
            Authorization: "Bearer invalidToken"
          expect:
            - statusCode: 404
            - hasProperty: 'message'
      - think: 1
      - log: "Test Case 2: Testing with no authorization token"
        post:
          url: "/checkout-cart-anonymous/{{ id }}"
          expect:
            - statusCode: 403
      - think: 1
      - log: "Test Case 3: Testing with invalid url"
        post:
          url: "/checkout-cart-anonymous-invalid/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: 'message'
