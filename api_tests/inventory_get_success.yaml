# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=a66f7079af

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "inventory_get_success.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Get pet inventories by status"
    flow:
      - log: "Sending GET request to /store/inventory"
        get:
          url: "/store/inventory"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasHeader: "content-type"
        think: 1
