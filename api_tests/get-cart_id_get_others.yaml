# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=ab533fff90

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Testing /get-cart/{id} endpoint"
    flow:
      - log: "Testing a case where Authorization header is missing"
        get:
          url: "/get-cart/123e4567-e89b-12d3-a456-426655440000"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1
      - log: "Testing a case where id is invalid"
        get:
          url: "/get-cart/invalid-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1
      - log: "Testing a case where id does not exist"
        get:
          url: "/get-cart/123e4567-e89b-12d3-a456-426655440001"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
      - log: "Testing a case where the Authorization token is invalid"
        get:
          url: "/get-cart/123e4567-e89b-12d3-a456-426655440000"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
            - hasProperty: "message"
