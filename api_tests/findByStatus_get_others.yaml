# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=560af8c1a1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "findByStatus_get_others.csv"
    fields: ["status"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Petstore - Find pets by status - Failure cases"
    flow:
      - log: "Sending a request with an invalid status value"
        get:
          url: "/pet/findByStatus?status=invalid_status"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Sending a request with status value missing"
        get:
          url: "/pet/findByStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1
      - log: "Sending a request with an invalid token"
        get:
          url: "/pet/findByStatus?status={{ status }}"
          headers:
            Authorization: "Invalid Token"
          expect:
            - statusCode: 401
        think: 1
      - log: "Sending a request to an invalid endpoint"
        get:
          url: "/pet/invalid_endpoint"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
        think: 1
