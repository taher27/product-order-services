# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=d9b91c8c5d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "checkout-cart-anonymous_id_post_success.csv"
    fields:
      - "id"
      - "currency"
      - "journeyType"
      - "contactPerson.contactInfo.address"
      - "contactPerson.contactInfo.city"
      - "contactPerson.contactInfo.country"
      - "contactPerson.contactInfo.email"
      - "contactPerson.contactInfo.mobile"
      - "contactPerson.contactInfo.zip"
      - "contactPerson.birthYear"
      - "contactPerson.birthMonth"
      - "contactPerson.birthDay"
      - "contactPerson.firstName"
      - "contactPerson.lastName"
      - "contactPerson.memberNumber"
      - "contactPerson.redeemableClubPoints"
      - "contactPerson.customerCode"
      - "contactPerson.gender"
      - "contactPerson.nationality"
      - "passengerDetails.0.contactInfo"
      - "passengerDetails.0.birthYear"
      - "passengerDetails.0.birthMonth"
      - "passengerDetails.0.birthDay"
      - "passengerDetails.0.firstName"
      - "passengerDetails.0.lastName"
      - "passengerDetails.0.memberNumber"
      - "passengerDetails.0.redeemableClubPoints"
      - "passengerDetails.0.customerCode"
      - "passengerDetails.0.gender"
      - "passengerDetails.0.nationality"
      - "passengerDetails.0.disabledPerson"
      - "passengerDetails.0.id"
      - "passengerDetails.0.code"
      - "passengerDetails.0.uuid"
      - "passengerDetails.0.bookedItemReference"
      - "outbound.journeyCode"
      - "outbound.arrivalPort"
      - "outbound.secondArrivalPort"
      - "outbound.departureCode"
      - "outbound.departurePort"
      - "outbound.secondDeparturePort"
      - "outbound.departureDate"
      - "outbound.arrivalDate"
      - "outbound.ticket.fareType"
      - "outbound.ticket.ticketCode"
      - "outbound.passengerPrices.0"
      - "outbound.cabins.0"
      - "outbound.extrasAshore.0"
      - "outbound.extrasAshoreWithOptions.0"
      - "outbound.extrasOnboard.0"
      - "outbound.meals.0"
      - "outbound.vehicles.0"
      - "outbound.vesselCode"
      - "inbound.journeyCode"
      - "inbound.arrivalPort"
      - "inbound.secondArrivalPort"
      - "inbound.departureCode"
      - "inbound.departurePort"
      - "inbound.secondDeparturePort"
      - "inbound.departureDate"
      - "inbound.arrivalDate"
      - "inbound.ticket.fareType"
      - "inbound.ticket.ticketCode"
      - "inbound.passengerPrices.0"
      - "inbound.cabins.0"
      - "inbound.extrasAshore.0"
      - "inbound.extrasAshoreWithOptions.0"
      - "inbound.extrasOnboard.0"
      - "inbound.meals.0"
      - "inbound.vehicles.0"
      - "inbound.vesselCode"
      - "hasVehicle"
      - "hasPet"
      - "agencyId"
      - "userId"
      - "connectionId"
      - "status"
      - "bookingResult.created"
      - "bookingResult.bookingCode"
      - "bookingResult.bookingState"
      - "bookingResult.bookingBalance"
      - "bookingResult.bookingTotal"
      - "bookingResult.paidState"
      - "bookingResult.currency"
      - "bookingResult.paymentUrl"
      - "bookingResult.paymentSessionId"
      - "bookingResult.bookedItems.groupPrices"
      - "bookingResult.bookedItems.outboundPassengerPrices"
      - "bookingResult.bookedItems.inboundPassengerPrices"
      - "bookingResult.bookedItems.outboundVehicles"
      - "bookingResult.bookedItems.inboundVehicles"
      - "bookingResult.bookedItems.outboundMeals"
      - "bookingResult.bookedItems.inboundMeals"
      - "bookingResult.bookedItems.outboundCabins"
      - "bookingResult.bookedItems.inboundCabins"
      - "bookingResult.bookedItems.outboundExtras"
      - "bookingResult.bookedItems.inboundExtras"
      - "bookingResult.bookedItems.outboundDiscounts"
      - "bookingResult.bookedItems.inboundDiscounts"
      - "timestamp"
      - "packageCode"
      - "referralCode"
      - "voucherCode"
      - "ticketBookCode"
      - "ticketBookPassword"
      - "accommodations.0.code"
      - "accommodations.0.name"
      - "accommodations.0.checkinDate"
      - "accommodations.0.checkoutDate"
      - "accommodations.0.rooms"
      - "showOnlyTotalPrice"
      - "paymentMethod"
    skipHeader: true
scenarios:
  - name: "Post Checkout Cart Anonymous"
    flow:
      - post:
          url: "/checkout-cart-anonymous/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            currency: "{{ currency }}"
            journeyType: "{{ journeyType }}"
            contactPerson:
              contactInfo:
                address: "{{ contactPerson.contactInfo.address }}"
                city: "{{ contactPerson.contactInfo.city }}"
                country: "{{ contactPerson.contactInfo.country }}"
                email: "{{ contactPerson.contactInfo.email }}"
                mobile: "{{ contactPerson.contactInfo.mobile }}"
                zip: "{{ contactPerson.contactInfo.zip }}"
              birthYear: "{{ contactPerson.birthYear }}"
              birthMonth: "{{ contactPerson.birthMonth }}"
              birthDay: "{{ contactPerson.birthDay }}"
              firstName: "{{ contactPerson.firstName }}"
              lastName: "{{ contactPerson.lastName }}"
              memberNumber: "{{ contactPerson.memberNumber }}"
              redeemableClubPoints: "{{ contactPerson.redeemableClubPoints }}"
              customerCode: "{{ contactPerson.customerCode }}"
              gender: "{{ contactPerson.gender }}"
              nationality: "{{ contactPerson.nationality }}"
            passengerDetails:
              - contactInfo: "{{ passengerDetails.0.contactInfo }}"
                birthYear: "{{ passengerDetails.0.birthYear }}"
                birthMonth: "{{ passengerDetails.0.birthMonth }}"
                birthDay: "{{ passengerDetails.0.birthDay }}"
                firstName: "{{ passengerDetails.0.firstName }}"
                lastName: "{{ passengerDetails.0.lastName }}"
                memberNumber: "{{ passengerDetails.0.memberNumber }}"
                redeemableClubPoints: "{{ passengerDetails.0.redeemableClubPoints }}"
                customerCode: "{{ passengerDetails.0.customerCode }}"
                gender: "{{ passengerDetails.0.gender }}"
                nationality: "{{ passengerDetails.0.nationality }}"
                disabledPerson: "{{ passengerDetails.0.disabledPerson }}"
                id: "{{ passengerDetails.0.id }}"
                code: "{{ passengerDetails.0.code }}"
                uuid: "{{ passengerDetails.0.uuid }}"
                bookedItemReference: "{{ passengerDetails.0.bookedItemReference }}"
            outbound:
              journeyCode: "{{ outbound.journeyCode }}"
              arrivalPort: "{{ outbound.arrivalPort }}"
              secondArrivalPort: "{{ outbound.secondArrivalPort }}"
              departureCode: "{{ outbound.departureCode }}"
              departurePort: "{{ outbound.departurePort }}"
              secondDeparturePort: "{{ outbound.secondDeparturePort }}"
              departureDate: "{{ outbound.departureDate }}"
              arrivalDate: "{{ outbound.arrivalDate }}"
              ticket:
                fareType: "{{ outbound.ticket.fareType }}"
                ticketCode: "{{ outbound.ticket.ticketCode }}"
              passengerPrices:
                - "{{ outbound.passengerPrices.0 }}"
              cabins:
                - "{{ outbound.cabins.0 }}"
              extrasAshore:
                - "{{ outbound.extrasAshore.0 }}"
              extrasAshoreWithOptions:
                - "{{ outbound.extrasAshoreWithOptions.0 }}"
              extrasOnboard:
                - "{{ outbound.extrasOnboard.0 }}"
              meals:
                - "{{ outbound.meals.0 }}"
              vehicles:
                - "{{ outbound.vehicles.0 }}"
              vesselCode: "{{ outbound.vesselCode }}"
            inbound:
              journeyCode: "{{ inbound.journeyCode }}"
              arrivalPort: "{{ inbound.arrivalPort }}"
              secondArrivalPort: "{{ inbound.secondArrivalPort }}"
              departureCode: "{{ inbound.departureCode }}"
              departurePort: "{{ inbound.departurePort }}"
              secondDeparturePort: "{{ inbound.secondDeparturePort }}"
              departureDate: "{{ inbound.departureDate }}"
              arrivalDate: "{{ inbound.arrivalDate }}"
              ticket:
                fareType: "{{ inbound.ticket.fareType }}"
                ticketCode: "{{ inbound.ticket.ticketCode }}"
              passengerPrices:
                - "{{ inbound.passengerPrices.0 }}"
              cabins:
                - "{{ inbound.cabins.0 }}"
              extrasAshore:
                - "{{ inbound.extrasAshore.0 }}"
              extrasAshoreWithOptions:
                - "{{ inbound.extrasAshoreWithOptions.0 }}"
              extrasOnboard:
                - "{{ inbound.extrasOnboard.0 }}"
              meals:
                - "{{ inbound.meals.0 }}"
              vehicles:
                - "{{ inbound.vehicles.0 }}"
              vesselCode: "{{ inbound.vesselCode }}"
            hasVehicle: "{{ hasVehicle }}"
            hasPet: "{{ hasPet }}"
            agencyId: "{{ agencyId }}"
            userId: "{{ userId }}"
            connectionId: "{{ connectionId }}"
            status: "{{ status }}"
            bookingResult:
              created: "{{ bookingResult.created }}"
              bookingCode: "{{ bookingResult.bookingCode }}"
              bookingState: "{{ bookingResult.bookingState }}"
              bookingBalance: "{{ bookingResult.bookingBalance }}"
              bookingTotal: "{{ bookingResult.bookingTotal }}"
              paidState: "{{ bookingResult.paidState }}"
              currency: "{{ bookingResult.currency }}"
              paymentUrl: "{{ bookingResult.paymentUrl }}"
              paymentSessionId: "{{ bookingResult.paymentSessionId }}"
              bookedItems:
                groupPrices: "{{ bookingResult.bookedItems.groupPrices }}"
                outboundPassengerPrices: "{{ bookingResult.bookedItems.outboundPassengerPrices }}"
                inboundPassengerPrices: "{{ bookingResult.bookedItems.inboundPassengerPrices }}"
                outboundVehicles: "{{ bookingResult.bookedItems.outboundVehicles }}"
                inboundVehicles: "{{ bookingResult.bookedItems.inboundVehicles }}"
                outboundMeals: "{{ bookingResult.bookedItems.outboundMeals }}"
                inboundMeals: "{{ bookingResult.bookedItems.inboundMeals }}"
                outboundCabins: "{{ bookingResult.bookedItems.outboundCabins }}"
                inboundCabins: "{{ bookingResult.bookedItems.inboundCabins }}"
                outboundExtras: "{{ bookingResult.bookedItems.outboundExtras }}"
                inboundExtras: "{{ bookingResult.bookedItems.inboundExtras }}"
                outboundDiscounts: "{{ bookingResult.bookedItems.outboundDiscounts }}"
                inboundDiscounts: "{{ bookingResult.bookedItems.inboundDiscounts }}"
            timestamp: "{{ timestamp }}"
            packageCode: "{{ packageCode }}"
            referralCode: "{{ referralCode }}"
            voucherCode: "{{ voucherCode }}"
            ticketBookCode: "{{ ticketBookCode }}"
            ticketBookPassword: "{{ ticketBookPassword }}"
            accommodations:
              - code: "{{ accommodations.0.code }}"
                name: "{{ accommodations.0.name }}"
                checkinDate: "{{ accommodations.0.checkinDate }}"
                checkoutDate: "{{ accommodations.0.checkoutDate }}"
                rooms: "{{ accommodations.0.rooms }}"
            showOnlyTotalPrice: "{{ showOnlyTotalPrice }}"
            paymentMethod: "{{ paymentMethod }}"
          expect:
            - statusCode: 200
            - hasProperty: "currency"
            - hasProperty: "journeyType"
            - hasProperty: "contactPerson"
            - hasProperty: "passengerDetails"
            - hasProperty: "outbound"
            - hasProperty: "inbound"
            - hasProperty: "hasVehicle"
            - hasProperty: "hasPet"
            - hasProperty: "agencyId"
            - hasProperty: "userId"
            - hasProperty: "connectionId"
            - hasProperty: "status"
            - hasProperty: "bookingResult"
            - hasProperty: "timestamp"
            - hasProperty: "packageCode"
            - hasProperty: "referralCode"
            - hasProperty: "voucherCode"
            - hasProperty: "ticketBookCode"
            - hasProperty: "ticketBookPassword"
            - hasProperty: "accommodations"
            - hasProperty: "showOnlyTotalPrice"
            - hasProperty: "paymentMethod"
          afterResponse: "think"
