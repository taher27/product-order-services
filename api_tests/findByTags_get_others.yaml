# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=20c7e46017

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "findByTags_get_others.csv"
    fields:
      - "tags"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Find pets by tags - failure and edge cases"
    flow:
      - log: "Testing with no tags"
        get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
              - "message"
              - "Invalid tag value"
      - think: 5

      - log: "Testing with non-existing tags"
        get:
          url: "/pet/findByTags?tags=nonExistingTag"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
              - "message"
              - "Invalid tag value"
      - think: 5

      - log: "Testing with invalid authorization"
        get:
          url: "/pet/findByTags?tags=tag1"
          headers:
            Authorization: "InvalidAuthToken"
          expect:
            - statusCode: 401
      - think: 5

      - log: "Testing with invalid URL"
        get:
          url: "/pet/findByInvalidTags?tags=tag1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5

      - log: "Testing with invalid method type"
        post:
          url: "/pet/findByTags?tags=tag1"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
