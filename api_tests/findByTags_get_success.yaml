# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=20c7e46017

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByTags_get_success.csv"
    fields: ["tags"]
    skipHeader: true

scenarios:
  - name: "Find pets by tags"
    flow:
      - get:
          url: "/pet/findByTags"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            tags: "{{ tags }}"
          expect:
            - statusCode: 200
            - hasProperty: "items"
          log: "Request to find pets by tags"
      - think: 1
