# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fbd3e66b3b

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_delete_others.csv"
    fields:
      - "api_key"
      - "petId"
  plugins:
    expect: {}
scenarios:
  - name: "Delete Pet API Failure and Edge Cases"
    flow:
      - log: "First request - Invalid URL"
        delete:
          url: "/invalidUrl/{{ petId }}"
          headers:
            Authorization: "InvalidAuthToken"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 404
        think: 1
      - log: "Second request - No petId in URL"
        delete:
          url: "/pet/"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1
      - log: "Third request - Invalid petId in URL"
        delete:
          url: "/pet/invalidPetId"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1
      - log: "Fourth request - No Authorization header"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            api_key: "{{ api_key }}"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1
      - log: "Fifth request - Missing api_key header"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
        think: 1
      - log: "Sixth request - Invalid api_key header"
        delete:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            api_key: "invalidApiKey"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - hasProperty: "code"
