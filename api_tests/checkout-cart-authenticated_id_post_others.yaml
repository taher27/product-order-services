# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=1205457fbf

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "checkout-cart-authenticated_id_post_others.csv"
    fields:
      - "id"
      - "Authorization"
    skipHeader: true
scenarios:
  - name: "Test /checkout-cart-authenticated/{id} endpoint"
    flow:
      - log: "Testing with Invalid ID"
        post:
          url: "/checkout-cart-authenticated/invalidId"
          headers:
            Authorization: "{{ Authorization }}"
          expect:
            - statusCode: 404
            - hasProperty: "message"
            - equals: 
                message: "Not Found"
      - think: 1
      - log: "Testing with Unauthorized access"
        post:
          url: "/checkout-cart-authenticated/{{ id }}"
          headers:
            Authorization: "Invalid Token"
          expect:
            - statusCode: 401
            - hasProperty: "message"
            - equals: 
                message: "Unauthorized"
      - think: 1
      - log: "Testing with Forbidden access"
        post:
          url: "/checkout-cart-authenticated/{{ id }}"
          headers:
            Authorization: "{{ Authorization }}"
          expect:
            - statusCode: 403
            - hasProperty: "message"
            - equals: 
                message: "Forbidden"
      - think: 1
      - log: "Testing with Bad Request"
        post:
          url: "/checkout-cart-authenticated/{{ id }}"
          headers:
            Authorization: "{{ Authorization }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
            - equals: 
                message: "Bad Request"
