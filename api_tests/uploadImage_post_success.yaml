# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=fd50cf6635

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "uploadImage_post_success.csv"
    fields: ["petId", "additionalMetadata", "file"]
    skipHeader: true

scenarios:
  - name: "Upload pet image scenario"
    flow:
      - post:
          url: "/pet/{{ petId }}/uploadImage"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "multipart/form-data"
          formData:
            additionalMetadata: "{{ additionalMetadata }}"
            file: "{{ file }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
          beforeRequest: "log"
          afterResponse: "think"

functions:
  log: 
    - "log": "Posting image for pet with id {{ petId }}"
  think:
    - "think": 1
