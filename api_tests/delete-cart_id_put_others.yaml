# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=75543754e4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "delete-cart_id_put_others.csv"
    fields:
      - "id"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for /delete-cart/{id} endpoint"
    flow:
      - log: "Testing with invalid id"
        put:
          url: "/delete-cart/invalid-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 5
      - log: "Testing without authorization"
        put:
          url: "/delete-cart/{{ id }}"
          headers:
            Authorization: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 5
      - log: "Testing with non-existent id"
        put:
          url: "/delete-cart/non-existent-id"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 5
      - log: "Testing with invalid URL"
        put:
          url: "/invalid-url/{{ id }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
