# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=560af8c1a1

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "findByStatus_get_success.csv"
    fields:
      - "status"
    skipHeader: true

scenarios:
  - name: "Find pets by status"
    flow:
      - get:
          url: "/pet/findByStatus"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          qs:
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - hasHeader: "content-type"
          beforeRequest:
            log: "Sending a GET request to /pet/findByStatus with status {{ status }}"
          afterResponse:
            think: 1
