# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=2db2056e97

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "pet_petId_get_success.csv"
    fields:
      - "petId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Find pet by ID"
    flow:
      - log: "Executing request to find a pet by ID"
        get:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          expect:
            - statusCode: 200
            - hasProperty: "name"
            - hasProperty: "photoUrls"
            - hasProperty: "tags"
            - hasProperty: "status"
        think: 1
