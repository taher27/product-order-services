# Test generated by RoostGPT for test test25OctPostmanCheck using AI Type Open AI and AI Model gpt-4

# RoostTestHash=99d9f02040

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "pet_petId_post_others.csv"
    fields:
      - "name"
      - "status"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Pet update failure scenario"
    flow:
      - log: "Sending request with invalid petId"
        post:
          url: "/pet/invalidId"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with missing form data"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid Content-Type"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "text/plain"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
      - log: "Sending request with invalid Authorization token"
        post:
          url: "/pet/{{ petId }}"
          headers:
            Authorization: "Invalid {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/x-www-form-urlencoded"
          formData:
            name: "{{ name }}"
            status: "{{ status }}"
          expect:
            - statusCode: 405
            - hasProperty: "message"
        think: 1
